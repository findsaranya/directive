import { BettterDirectiveDirective } from './bettter-directive.directive';

describe('BettterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BettterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
